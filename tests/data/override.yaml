---
sw-spine-001:
- lineage:
  - equals:
    - ssh server vrf default
    - ssh server vrf mgmt
    - no ip icmp redirect
    - https-server vrf default
    - https-server vrf mgmt
  add_tags: override
- lineage:
  - startswith: vsx
  add_tags: override

sw-spine-002:
- lineage:
  - startswith: interface 1
  - startswith:
    - description
  add_tags: override

- lineage:
  - startswith: interface 1
  - startswith:
    - lag
  add_tags: override

sw-leaf-001:
- lineage:
  - startswith: vsx
  add_tags: override
sw-leaf-002:
- lineage:
  - startswith: vsx
  add_tags: override
sw-leaf-003:
- lineage:
  - startswith: vsx
  add_tags: override
sw-leaf-004:
- lineage:
  - startswith: vsx
  add_tags: override
sw-cdu-001:
- lineage:
  - startswith: vsx
  add_tags: override
sw-cdu-002:
- lineage:
  - startswith: vsx
  add_tags: override
sw-leaf-bmc-001:
- lineage:
  - startswith: snmp
  add_tags: override