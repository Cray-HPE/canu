{#- spine-to-leaf-bmc #}
{%- for node in cabling.nodes if node.subtype == "leaf-bmc" %}
interface lag {{ node.config.LAG_NUMBER }} multi-chassis
    no shutdown
    description {{ node.config.DESCRIPTION }}
    no routing
    vlan trunk native {{ variables.NATIVE_VLAN }}
{%- if variables.RVR_NMN_NODES  %}
    vlan trunk allowed {{ groupby_vlan_range(variables.NMN_RVR_VLAN_LIST + variables.LEAF_BMC_VLANS) }}
{%- else %}
    vlan trunk allowed {{ groupby_vlan_range(variables.LEAF_BMC_VLANS) }}
{%- endif %}
    lacp mode active

interface 1/1/{{ node.config.PORT }}
    no shutdown
    mtu 9198
    description {{ node.config.DESCRIPTION }}
    lag {{ node.config.LAG_NUMBER }}

{%- endfor %}
{#- end spine-to-leaf-bmc #}
