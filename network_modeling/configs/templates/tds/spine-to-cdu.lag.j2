{#- spine-to-cdu.lag #}
***===> sw-spine-to-cdu.lag.j2
{%- for node in cabling.nodes if node.subtype == "cdu" %}
interface lag ***===> {{ node.config.LAG_NUMBER }} multi-chassis
    no shutdown
    description ***===> {{ node.config.DESCRIPTION }}
    no routing
    vlan trunk native 1
    vlan trunk allowed 2,4
    lacp mode active
    spanning-tree root-guard

interface ***===> {{ node.config.PORT }}
    no shutdown
    mtu 9198
    description ***===> {{ node.config.DESCRIPTION }}
    lag ***===> {{ node.config.LAG_NUMBER }}

{%- endfor %}
***===> end sw-spine-to-cdu.lag.j2
{#- end spine-to-cdu.lag #}