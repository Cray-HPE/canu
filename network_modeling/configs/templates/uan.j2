{#
uan
#}
{% for node in cabling.nodes %}
{% if node.subtype == "uan" %}
interface lag {{ node.config.lag_number }} multi-chassis
no shutdown
description {{ node.config.description }}
no routing
vlan trunk native 1
vlan trunk allowed 1-2,4,7
lacp mode active
lacp fallback
spanning-tree port-type admin-edge

interface {{ node.config.interface_lag}}
no shutdown
mtu 9198
description {{ node.config.description }}    
interface lag {{ node.config.lag_number }}

interface {{ node.config.interface_nmn }}
no shutdown
mtu 9198
no routing
vlan access 2
description {{ node.config.description }}
spanning-tree bpdu-guard
spanning-tree port-type admin-edge
{% endif %}
{% endfor %}
{#
end-uan
#}